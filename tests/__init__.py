"""
tests 包初始化。

本目录放置单元测试（unittest 标准库，零外部依赖）。
所有测试文件应以 `test_` 开头命名。

运行方式（在项目根目录）：
    python -m unittest discover -s tests -v
    # 或单文件：
    python -m unittest tests.test_parse_precondition -v
"""
